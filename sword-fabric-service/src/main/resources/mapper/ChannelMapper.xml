<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.kkgy333.sword.fabric.server.dao.mapper.ChannelMapper">

    <select id="check" resultType="com.github.kkgy333.sword.fabric.server.dao.Channel">
		select id,name,block_listener,callback_location,peer_id,date from channel where name=#{c.name} and peer_id=#{c.peerId}
	</select>
</mapper>